package com.devpro.resto.utils.dummies

import com.devpro.resto.utils.models.Menus

object DataMenu {
    fun getData(): ArrayList<Menus> {
        val menus = arrayListOf<Menus>()
        return menus
    }
}